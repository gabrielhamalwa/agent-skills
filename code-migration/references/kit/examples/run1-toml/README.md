# run1-toml: preserved run artifacts

These are RUN ARTIFACTS preserved from the kit's first dogfood — migrating toml 0.10.2 (PyPI, MIT, by uiri and contributors; analyzed, never vendored) — not runnable demos.
To point the parity harness (`py_side.py` / `node_side.mjs`) at a local clone, set `TOML_SRC` (Python source checkout) and `TOML_TS_DIST` (built TypeScript dist).
For the receipts behind these artifacts, see [../../RUN-NOTES.md](../../RUN-NOTES.md).
