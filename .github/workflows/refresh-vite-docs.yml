name: refresh-vite-docs

on:
  schedule:
    - cron: "32 6 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Refresh Vite docs mirror
        run: bash vite/scripts/refresh-docs.sh

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add vite/references
          git diff --cached --quiet || git commit -m "chore(vite): refresh docs mirror"
          git push
